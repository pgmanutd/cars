import React from 'react';
import { Helmet } from 'react-helmet';

const Cars: React.FC = () => (
  <section data-testid="Cars">
    <Helmet>
      <title>Cars</title>
    </Helmet>
  </section>
);

export default Cars;
