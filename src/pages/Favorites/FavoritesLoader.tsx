import React from 'react';
import Box from '@material-ui/core/Box';

const FavoritesLoader: React.FC = () => (
  <Box component="section" data-testid="FavoritesLoader">
    Loading...
  </Box>
);

export default FavoritesLoader;
